self.addEventListener('install', (e) => {
  e.waitUntil(
    caches.open('all-logic-cache').then((cache) => cache.addAll([
      '/',
      '/index.html',
      // Add other assets
    ]))
  );
});

self.addEventListener('fetch', (e) => {
  e.respondWith(
    caches.match(e.request).then((response) => response || fetch(e.request))
  );
});